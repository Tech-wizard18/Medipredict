services:
  - type: web
    name: medipredict
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn disease_app.wsgi:application --bind 0.0.0.0:$PORT"
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: disease_app.settings.production
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: DJANGO_ALLOWED_HOSTS
        value: .onrender.com
      - key: CSRF_TRUSTED_ORIGINS
        value: https://*.onrender.com
      - key: DEBUG
        value: "False"
      - key: DATABASE_URL
        fromDatabase:
          name: medipredict-db
          property: connectionString

databases:
  - name: medipredict-db
    databaseName: medipredict
    user: medipredict
